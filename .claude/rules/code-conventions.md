---
paths:
  - "src/**/*.{ts,tsx}"
---

# Code Conventions

- TypeScript strict mode — all shared interfaces in `src/interfaces/`
- Prettier: 100 char width, single quotes, trailing commas (es5), bracket same line
- ESLint: extends `eslint-config-expo`, `react/display-name` rule disabled
